[project]
name = "glassdoor-analysis"
version = "0.1.0"
description = "CLI tool for collecting Glassdoor aggregate review metrics"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

